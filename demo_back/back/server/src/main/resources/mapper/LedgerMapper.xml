<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.LedgerMapper">

    <insert id="addLedger">
        insert into ledger(ledger_name, create_time) values (#{ledgerName},#{createTime})
    </insert>
</mapper>
