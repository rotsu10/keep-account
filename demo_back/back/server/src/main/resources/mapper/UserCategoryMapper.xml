<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserCategoryMapper">


    <select id="queryCategoryType" resultType="java.lang.Integer">
        select id,user_id,name from category where category.user_id = #{userId} and id = #{categoryId}
    </select>
</mapper>
